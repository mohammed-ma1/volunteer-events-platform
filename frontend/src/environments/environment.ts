export const environment = {
  production: true,
  apiUrl: '/api',
  tapPreferFullPageRedirectOnLocalhost: false,
  /** Meta (Facebook) Pixel ID. Empty string disables the pixel entirely. */
  metaPixelId: '25252667454407472',
  /**
   * Coming-soon mode. When true, every route redirects to a "site available soon"
   * landing page. Set to false to open the full site to the public.
   */
  comingSoon: true,
};
