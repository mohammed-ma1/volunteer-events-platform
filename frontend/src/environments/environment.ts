export const environment = {
  production: true,
  apiUrl: '/api',
  tapPreferFullPageRedirectOnLocalhost: false,
  /** When true, the public storefront is replaced by a temporary unavailable page. */
  siteDisabled: true,
  /** Meta (Facebook) Pixel ID. Empty string disables the pixel entirely. */
  metaPixelId: '25252667454407472',
};
