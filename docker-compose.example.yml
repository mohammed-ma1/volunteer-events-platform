# Copy patterns from docker-compose.yml in the repo root (MySQL for local dev).
# PostgreSQL example (not used by current Laravel .env):
#
# services:
#   db:
#     image: postgres:16-alpine
#     environment:
#       POSTGRES_DB: volunteer_events
#       POSTGRES_USER: ve
#       POSTGRES_PASSWORD: ve_local
#     ports:
#       - "5432:5432"
#     volumes:
#       - ve_pgdata:/var/lib/postgresql/data
# volumes:
#   ve_pgdata:
